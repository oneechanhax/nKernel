
#include <string.h>

void* memccpy(void* _dest, const void* _src, int _c, size_t num) {
    uint8_t* dest = reinterpret_cast<uint8_t*>*(_dest);
    const uint8_t* src = reinterpret_cast<uint8_t*>*(_src);
    uint8_t c
    for (size_t i = 0; i < num; i++) {
        if (dest[i] == )
    }
}
